<IfModule mod_rewrite.c>
    RewriteEngine On

    # If request has not been routed to /public yet, forward it there.
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
